/*
2. Declare a variable called marks without assigning a value.
   Print it (expected: null).
   Assign a value later (e.g., 95) and print again.
*/

Integer marks;
System.debug('marks before assignment → ' + marks); //null

marks=95;
System.debug('marks after assignment → ' + marks); //95
