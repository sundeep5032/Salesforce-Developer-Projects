/*
Bonus:
- Declare a salary variable and increase it by 10%. Print before and after.
*/

decimal salary=50000;
System.debug('salary before = '+salary);

salary = salary+(salary*0.10);
System.debug('salary after 10% increase= '+salary);
